<!DOCTYPE html>
<html>
<head>
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <h1><a href="/">My Blog</a></h1>
    </header>
    <main>
        <article>
            <h1>{{ title }}</h1>
            <time datetime="{{ date | date('YYYY-MM-DD') }}">
                {{ date | dateDisplay }}
            </time>
            {{ content | safe }}
        </article>
    </main>
</body>
</html>